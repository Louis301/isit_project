## Для подготовки виртуального окружения используются след. команды:

- py -m venv venv
- .\venv\Script\avtivate
- py -m pip install Django
- py -m pip install gigachat

(API-ключ gigachat может быть не действителен!)
